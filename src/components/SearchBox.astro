---
const { placeholder = "搜尋筆記、文章、系列" } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<form class="search-box" role="search" method="get" action={`${base}search/`}>
  <label class="visually-hidden" for="site-search">搜尋</label>
  <input
    id="site-search"
    type="search"
    name="q"
    placeholder={placeholder}
    autocomplete="off"
  />
  <button type="submit">搜尋</button>
  <p class="search-hint">輸入關鍵字後按 Enter。</p>
</form>
